<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Administration des Photos</title>
    <style>
        /* Styles CSS basiques pour la mise en forme */
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            margin-bottom: 20px;
        }
        .add-photo {
            margin-bottom: 20px;
        }
        .add-photo a {
            text-decoration: none;
            background-color: #28a745;
            color: #fff;
            padding: 10px 15px;
            border-radius: 5px;
        }
        .add-photo a:hover {
            background-color: #218838;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table thead {
            background-color: #f8f9fa;
        }
        table th, table td {
            border: 1px solid #dee2e6;
            padding: 12px;
            text-align: left;
        }
        table img {
            max-width: 150px;
            height: auto;
        }
        .action-links a {
            margin-right: 10px;
            color: #dc3545;
            text-decoration: none;
        }
        .action-links a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
<h1>Administration des Photos</h1>

<div class="add-photo">
    <a href="{{ path('admin_photos_add') }}">Ajouter une nouvelle photo</a>
</div>

{% if photos is not empty %}
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Titre</th>
            <th>Image</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for photo in photos %}
            <tr>
                <td>{{ photo.id }}</td>
                <td>{{ photo.title }}</td>
                <td>
                    <img src="{{ photo.imagePath }}" alt="{{ photo.title }}">
                </td>
                <td class="action-links">
                    <a href="{{ path('admin_photos_delete', {'id': photo.id}) }}" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette photo ?');">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    <p>Aucune photo n'a été trouvée.</p>
{% endif %}
</body>
</html>
